let coord = e.clientX - scrollMinPos;
            
            if(e.clientX > scrollMinPos) {
                if(scrollMax.offsetLeft - coord < 25)
                    return 1;
                
                target.style.left = `${coord}px`; 
                // target.style.transform = `translateX(${coord}px)`;
            } else {
                if(coord < 0)
                    return 1;
                
                target.style.left = `${coord}px`; 
                // target.style.transform = `translateX(${coord}px)`;
            }


let scrollMinPos;
    let scrollMaxPos;


if(target.matches('.skroll_min')) {
            
        }
        
        if(target.matches('.skroll_max')) {
            
        }

        if(target.matches('.skroll_min')) 
            scrollMinPos == undefined ? scrollMinPos = e.clientX : false;
        

        if(target.matches('.skroll_max'))
            scrollMaxPos == undefined ? scrollMaxPos = e.clientX : false


if(target.matches('.skroll_min'))
            scrollMinPos = e.clientX;

        if(target.matches('.skroll_max'))
            scrollMaxPos = e.clientX;


            let coord = e.clientX - scrollMaxPos;
            if(e.clientX > scrollMaxPos) {
                target.style.transform = `translateX(${coord}px)`;
            } else {
                console.log(-coord - scrollMin.offsetLeft);
                target.style.transform = `translateX(${coord}px)`;
            }